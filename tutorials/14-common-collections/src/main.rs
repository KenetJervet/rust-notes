#![allow(unused_variables)]
#![allow(unused_mut)]

mod vectors;
mod strings;
mod hashmaps;

use vectors::vectors;
use strings::strings;
use hashmaps::hashmaps;

fn main() {
    vectors();
    strings();
    hashmaps();
}
